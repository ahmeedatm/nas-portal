/**
 * NAS Portal - Global Configuration
 * Edit this file to change your host or check parameters.
 */
const CONFIG = {
  // How often to check service status (in milliseconds)
  CHECK_INTERVAL: 30000,
  
  // Maximum time to wait for a service to respond (in milliseconds)
  FETCH_TIMEOUT: 3000,

  // Base URL for dashboard icons
  ICON_BASE_URL: 'https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons/png/'
};
